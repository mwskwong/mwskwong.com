generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ArticleMetadata {
  id   String @id
  view Int    @default(0)

  @@map("article_metadata")
}
