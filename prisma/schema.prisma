generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "mysql"
  relationMode = "prisma"
  url          = env("DATABASE_URL")
}

model BlogMetadata {
  id   String @id
  view Int    @default(0)
  like Int    @default(0)
}
