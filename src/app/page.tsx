import { Button } from '@radix-ui/themes';
import { type FC } from 'react';

const Home: FC = () => {
  return (
    <div>
      <Button>This is a button</Button>
    </div>
  );
};

export default Home;
